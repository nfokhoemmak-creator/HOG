{
  "name": "goodest-chews-theme",
  "version": "1.0.0",
  "description": "Custom Shopify Online Store 2.0 theme for Goodest Chews (goodestchews.com) — a one-product store for 8-in-1 multivitamin soft chews for dogs",
  "private": true,
  "license": "MIT",
  "homepage": "https://goodestchews.com",
  "scripts": {
    "dev": "shopify theme dev",
    "check": "shopify theme check",
    "check:fix": "shopify theme check --auto-correct",
    "pull": "shopify theme pull",
    "push": "shopify theme push --unpublished",
    "deploy": "shopify theme push --live",
    "package": "shopify theme package",
    "validate:json": "node scripts/validate-json.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@shopify/cli": "^3.60.0"
  }
}
